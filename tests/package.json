{
  "name": "migracja-testy",
  "private": true,
  "type": "module",
  "scripts": {
    "capture:old": "node capture.mjs --base http://localhost:3000 --out snapshots/old",
    "capture:new": "node capture.mjs --base http://localhost:8080 --out snapshots/new",
    "check": "node diff-visual.mjs && node diff-content.mjs"
  },
  "dependencies": {
    "pixelmatch": "^6.0.0",
    "playwright": "^1.50.0",
    "pngjs": "^7.0.0"
  }
}
